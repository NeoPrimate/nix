(defcfg
  process-unmapped-keys yes
  macos-dev-names-include ("Apple Internal Keyboard / Trackpad")
)

(defsrc
  caps ralt
)

(defalias
  hyper (multi lctl lalt lsft lmet)
  meh   (multi lctl lalt lsft)
)

(deflayer base
  (tap-hold 200 200 esc  @hyper)
  (tap-hold 200 200 ralt @meh)
)
