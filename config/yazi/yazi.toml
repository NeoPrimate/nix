# [opener]
# edit = [
#     { run = 'hx "$@"', block = true, desc = "Helix" }
# ]

# [open]
# rules = [
#     { name = "*/", use = "edit" }, # Matches directories (optional)
#     { mime = "text/*", use = "edit" },
#     { mime = "application/javascript", use = "edit" },
#     { mime = "application/json", use = "edit" },
#     # Add a fallback for files without an explicit mime type
#     { name = "*", use = "edit" }
# ]
